// Things I need

import React from "react";
//import components
//render function to display components

//character cards need a way to be shuffled 
    //(for loop)

//Game needs to keep track of points
    // When user clicks, save value of card
    // If previous value is the same as current value,
        //lose game
    //else
        //keep playing.

//Set High Score Variable.
    //Compare user score to high score.
    //If user score becomes higher than "High Score"
        //update high score

//export the app